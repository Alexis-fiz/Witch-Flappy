// Previous pipe gap position (initialize at middle)
previous_gap_center = room_height / 2;  // 512 for 1024 height

// Max change between consecutive pipes
max_gap_change = 60;  // Adjust this value (40-100 works well)

interval_Time = 124;
alarm[0] = interval_Time;